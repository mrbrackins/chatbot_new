# ChatScreen

ChatScreen is a clean, responsive chat UI component built with **React** and **TypeScript**, styled entirely using **custom CSS with no UI frameworks**.  
This project demonstrates hand-crafted component design, layout structure, and modern responsive styling without relying on libraries like Tailwind, Bootstrap, or Material UI.

## 🚀 Features

- Custom-built CSS (no frameworks)
- Responsive, modern chat UI
- Scrollable message area
- Reusable React + TypeScript component
- Easy to integrate into any app

## 📂 Project Structure

```text
src/
  components/
    ChatScreen.tsx
    ChatScreen.css
  App.tsx
  main.tsx
```

## 🛠️ Installation

```bash
git clone https://github.com/your-username/ChatScreen.git
cd ChatScreen
npm install
npm run dev
```

## 🧩 Usage Example

```tsx
import ChatScreen from "./components/ChatScreen";

function App() {
  return <ChatScreen />;
}

export default App;
```

## 📌 Future Enhancements

- Message input + send bar
- Emoji / attachment support
- Real-time chat integration (e.g., Firebase, Socket.io)
- Dark mode

## 📄 License

MIT License.
